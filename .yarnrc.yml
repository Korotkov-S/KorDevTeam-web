compressionLevel: mixed

enableGlobalCache: false

nodeLinker: node-modules

# Build multi-platform images without executing Node/Yarn under QEMU. Keep the
# native optional packages required by both Alpine runtime architectures.
supportedArchitectures:
  os: [current, linux]
  cpu: [current, x64, arm64]
  libc: [current, musl]
