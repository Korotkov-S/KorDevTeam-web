version: "3.8"

services:
  kordevteam-registry:
    image: ghcr.io/korotkov-s/kordevteam-web:latest
    ports:
      - "8081:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
